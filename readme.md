* **use .net 4.7**
* for run with sc.exe

```
sc create `<SERVICE-NAME>` binpath="<ncService.exe PATH>"
```

* after that from srvicess make it automatic
* than connect with

```
nc IP-ADDRESS 8555
```
